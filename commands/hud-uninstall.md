Uninstall macm-hud. Ask the user to confirm first. If confirmed, ask for the path where they cloned the repo, then run:

```
python <repo_path>/install.py --uninstall
```

If they don't know the path, tell them to run: `python -c "import pathlib; print('Repo path needed — where did you clone macm-hudclaudecodeskill?')"`
